<nav class="navbar navbar-default <%= "navbar-inverse" unless Rails.env.production? %>">
  <div class="container">

    <div class="navbar-header">
      <%= link_to "IDeATe Quartermaster", root_path, class: "navbar-brand" %>
    </div>

    <ul class="nav navbar-nav">
      <li>
        <%= link_to 'Items', items_path %>
      </li>
    </ul>

    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          <span class="glyphicon glyphicon-user"></span> <%= current_user.email %><b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li>
              <%= link_to destroy_user_session_path, :method => 'delete', :id => 'log_out' do %>
                <span class="glyphicon glyphicon-log-out"></span> Logout
              <% end %>
            </li>
          </ul>
        </li>
      <% else %>
        <li>
          <%= link_to new_user_session_path, :id => 'log_in' do %>
            <span class="glyphicon glyphicon-log-in"></span> Login
          <% end %>
        </li>
      <% end %>
    </ul>

  </div>
</nav>
