<% if can? :create, @item %>
<p>
  <%= button_to 'New item', new_item_path, method: :get, class: 'btn btn-primary' %>
</p>
<% end %>

<ul>
  <% @items.each do |item| %>
    <li>
      <% if item.active %>
        <%= link_to item.name, item_path(item.id) %>
      <% else %>
        <s><%= link_to item.name, item_path(item.id), class: 'text-muted'  %></s>
      <% end %>
    </li>
  <% end %>
</ul>

<% if can? :create, @item %>
<p>
  <%= button_to 'New item', new_item_path, method: :get, class: 'btn btn-primary' %>
</p>
<% end %>
